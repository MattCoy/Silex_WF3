<!doctype html>
<html>
<head>
    <meta charset="utf-8" />
    <link href="{{ asset('assets/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet" />
    <title>Silex - Home</title>
</head>
<body>
    <div class="container">
        <header>
            <h1>Silex</h1>
        </header>
        <div class="col-md-12">
            {% for article in articles %}
            <article>
                <h2>{{ article.title }}</h2>
                <p>{{ article.content }}</p>
                <p>publié le {{ article.date_publi }}</p>
            </article>
            {% endfor %}
        </div>
        <footer class="footer">
        </footer>
    </div>
</body>
</html>